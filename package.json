{
  "name": "vue-stock-dashboard-manager",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "front": "npm --prefix frontend run dev",
    "back": "bash -c 'cd backend && source venv/bin/activate && uvicorn main:app --reload'",
    "dev": "concurrently \"npm run front\" \"npm run back\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
